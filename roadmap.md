# 🗺️ نقشه راه مشترک یادگیری DevOps و Data Science

> این نقشه راه شامل مراحلی است که پیش از تخصصی شدن در DevOps یا Data Science، باید بلد باشی.
> از چک‌باکس‌ها برای ردیابی پیشرفتت استفاده کن.

---

## مرحله ۱: لینوکس و شبکه 🐧

پایه‌ای‌ترین مهارت. تقریباً همه‌ی سرورها، کانتینرها و محیط‌های تولید داده روی لینوکس اجرا می‌شن.

**شاخه‌های این مرحله:**
- [ ] مدیریت فایل و دایرکتوری: `ls`, `cd`, `cp`, `mv`, `rm`, `find`, `grep`
- [ ] مدیریت پرمیشن‌ها و کاربران: `chmod`, `chown`, `sudo`, `useradd`
- [ ] مدیریت پروسس‌ها: `ps`, `top`, `htop`, `kill`, `systemctl`
- [ ] Bash Scripting: نوشتن اسکریپت‌های خودکار با شرط، حلقه و متغیر
- [ ] مفاهیم شبکه: IP، Subnet، DNS، پورت، HTTP/HTTPS
- [ ] SSH: اتصال امن به سرور ریموت و مدیریت کلید (`ssh-keygen`)
- [ ] فایروال پایه: `ufw` یا `iptables`

**چرا مهمه:** بدون تسلط به ترمینال، هیچ کدام از مراحل بعدی (Docker، Cloud، سرورهای مدل ML) قابل انجام نیستن.

---

## مرحله ۲: پایتون 🐍

زبان مشترک هر دو مسیر — هم برای اسکریپت‌نویسی DevOps و هم پایه‌ی کامل Data Science.

**شاخه‌های این مرحله:**
- [ ] Syntax پایه: متغیرها، حلقه‌ها (`for`, `while`)، شرط‌ها
- [ ] ساختارهای داده: `list`, `dict`, `set`, `tuple`
- [ ] توابع و مفهوم Scope
- [ ] برنامه‌نویسی شیءگرا (OOP): کلاس، وراثت، Polymorphism
- [ ] مدیریت خطا: `try / except / finally`
- [ ] محیط مجازی: `venv` و مدیریت پکیج با `pip`
- [ ] کتابخانه‌های استاندارد: `os`, `sys`, `json`, `datetime`, `requests`
- [ ] (اختیاری برای شروع مسیر DS) آشنایی مقدماتی با `NumPy` و `Pandas`

**چرا مهمه:** تقریباً هر ابزار اتوماسیون (Ansible, Terraform scripts) و هر مدل یادگیری ماشین با پایتون نوشته می‌شه.

---

## مرحله ۳: Git و کنترل نسخه 🔀

بدون Git نمی‌تونی روی هیچ پروژه‌ی واقعی (چه کد چه مدل داده) به شکل حرفه‌ای کار کنی.

**شاخه‌های این مرحله:**
- [ ] دستورات پایه: `git init`, `add`, `commit`, `push`, `pull`, `clone`
- [ ] شاخه‌بندی: `branch`, `merge`, `rebase`
- [ ] حل تعارض (Conflict Resolution)
- [ ] Workflow گیت‌هاب: `fork`, `Pull Request`, `Code Review`
- [ ] `.gitignore` و مدیریت فایل‌های حساس (مثل API Key)
- [ ] Tag و Release گذاری نسخه‌ها

**چرا مهمه:** کل نقشه راهی که داری می‌سازی هم قراره داخل همین ابزار مدیریت بشه.

---

## مرحله ۴: SQL و پایگاه داده 🗄️

زبان استاندارد برای صحبت با داده‌ها، چه برای مانیتورینگ سیستم (DevOps) چه تحلیل داده (DS).

**شاخه‌های این مرحله:**
- [ ] دستورات پایه: `SELECT`, `WHERE`, `ORDER BY`, `LIMIT`
- [ ] `JOIN`ها: Inner, Left, Right, Full
- [ ] تجمیع داده: `GROUP BY`, `HAVING`, توابع Aggregate
- [ ] Subquery و CTE (`WITH`)
- [ ] Indexing و بهینه‌سازی کوئری
- [ ] آشنایی مفهومی با NoSQL: MongoDB (Document-based)، Redis (Key-Value)

**چرا مهمه:** لاگ‌های سیستم، متریک‌ها، و دیتاست‌های تحلیلی معمولاً در دیتابیس ذخیره می‌شن.

---

## مرحله ۵: Docker 🐳

پایه بسیاری از پروژه‌های واقعی مدرن — چه دیپلوی یک سرویس، چه اجرای یک مدل ML.

**شاخه‌های این مرحله:**
- [ ] مفهوم Image در برابر Container
- [ ] نوشتن `Dockerfile`
- [ ] دستورات اصلی: `build`, `run`, `ps`, `exec`, `logs`
- [ ] `Docker Compose` برای اجرای چند سرویس همزمان
- [ ] Volume (ذخیره‌سازی داده) و Networking بین کانتینرها
- [ ] Docker Hub: Push/Pull ایمیج‌ها

**چرا مهمه:** هر برنامه‌ای که بسازی (وب سرویس یا مدل ML) باید قابل حمل و اجرا در هر محیطی باشه — Docker این رو تضمین می‌کنه.

---

## مرحله ۶: API و وب سرویس 🌐

نقطه اتصال بین کد، داده و کاربر.

**شاخه‌های این مرحله:**
- [ ] مفهوم REST و متدهای HTTP: `GET`, `POST`, `PUT`, `DELETE`
- [ ] ساخت API ساده با `Flask` یا `FastAPI`
- [ ] احراز هویت پایه: API Key، آشنایی مقدماتی با JWT
- [ ] تست و مصرف API با `requests` یا Postman
- [ ] مستندسازی API با Swagger/OpenAPI

**چرا مهمه:** در DevOps برای مانیتورینگ سرویس‌ها و در DS برای سرو کردن مدل (Model Serving) کاربرد مستقیم داره.

---

## مرحله ۷: Cloud پایه ☁️

**شاخه‌های این مرحله:**
- [ ] مفاهیم IaaS / PaaS / SaaS
- [ ] Compute: ساخت و مدیریت یک ماشین مجازی (EC2 در AWS یا معادلش)
- [ ] Storage: ذخیره فایل (S3 یا معادل)
- [ ] IAM: مدیریت دسترسی و نقش‌ها
- [ ] Networking پایه در Cloud: VPC، Security Group
- [ ] پیشنهاد: شروع با **AWS Free Tier** (رایگان با محدودیت ماهانه، نه Trial کوتاه‌مدت)

**چرا مهمه:** تقریباً هر پروژه واقعی امروز روی یکی از پلتفرم‌های ابری دیپلوی می‌شه.

---

## مرحله ۸: CI/CD پایه ⚙️

**شاخه‌های این مرحله:**
- [ ] مفهوم Pipeline: Build → Test → Deploy
- [ ] نوشتن Workflow در GitHub Actions (فایل‌های `.yml`)
- [ ] Triggerها: `on: push`, `on: pull_request`
- [ ] مدیریت Secrets در پایپ‌لاین
- [ ] مثال عملی: اجرای تست خودکار پایتون هنگام هر Push

**چرا مهمه:** خودکارسازی تست و دیپلوی، هسته اصلی کار DevOps و همچنین بخش MLOps در Data Science است.

---

## 🔀 نقطه جدایی مسیرها

بعد از این ۸ مرحله، مسیرها تخصصی می‌شن:

**مسیر DevOps:**
Kubernetes → Terraform (Infrastructure as Code) → Ansible → مانیتورینگ (Prometheus/Grafana) → Security پایه

**مسیر Data Science:**
آمار و احتمال → Pandas/NumPy پیشرفته → بصری‌سازی داده (Matplotlib/Seaborn) → Machine Learning (Scikit-learn) → Deep Learning پایه

---
